# AI PR Review Workflow - Ollama Powered
# Uses GLM-4.6 Cloud model via Ollama for cost-effective AI reviews
#
# Synced from jbcom-control-center (always-sync)
# DO NOT EDIT - changes will be overwritten

name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write

# Prevent multiple reviews on rapid commits
concurrency:
  group: pr-review-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  # ==========================================================================
  # Ollama AI Review - GLM-4.6 Cloud Model
  # Cost-effective, high-quality code reviews
  # ==========================================================================
  ollama-review:
    name: AI Review (GLM-4.6)
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'pull_request' &&
      !github.event.pull_request.draft &&
      github.actor != 'dependabot[bot]'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Ollama
        uses: ai-action/setup-ollama@v1
        with:
          model: glm-4.6:cloud

      - name: Get PR Diff
        id: diff
        run: |
          gh pr diff ${{ github.event.pull_request.number }} > /tmp/pr_diff.txt
          echo "diff_file=/tmp/pr_diff.txt" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: AI Review
        uses: ai-action/ollama-action@v1
        id: review
        with:
          model: glm-4.6:cloud
          prompt: |
            You are a senior code reviewer. Analyze this pull request diff and provide a comprehensive review.

            **PR Title:** ${{ github.event.pull_request.title }}
            **Author:** ${{ github.event.pull_request.user.login }}
            **Repository:** ${{ github.repository }}

            **Diff:**
            $(cat /tmp/pr_diff.txt | head -2000)

            **Review Instructions:**
            1. **Security** - Look for vulnerabilities, exposed secrets, injection risks
            2. **Performance** - Identify bottlenecks, memory leaks, expensive operations
            3. **Code Quality** - Check readability, DRY violations, complexity
            4. **Testing** - Note missing test coverage or edge cases
            5. **Best Practices** - Verify error handling, documentation, patterns

            **Output Format:**
            ## Summary
            [1-2 sentence overall assessment]

            ## Score: [0-100]

            ## Issues Found
            | Severity | File | Issue |
            |----------|------|-------|
            | HIGH/MED/LOW | path | description |

            ## Recommendations
            - [Actionable suggestions]

            If the PR looks good, say so briefly and give a high score.
        env:
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}

      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const review = `${{ steps.review.outputs.response }}`;
            
            const body = `## 🤖 AI Code Review (GLM-4.6)

            ${review}

            ---
            <sub>Powered by Ollama GLM-4.6 Cloud • [View workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})</sub>`;

            // Check for existing review comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
            });

            const existingComment = comments.find(c => 
              c.user.type === 'Bot' && 
              c.body.includes('🤖 AI Code Review')
            );

            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body: body
              });
            }

  # ==========================================================================
  # Mention-triggered reviews (/review)
  # ==========================================================================
  mention-review:
    name: Mention-triggered Review
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      contains(github.event.comment.body, '/review')

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Ollama
        uses: ai-action/setup-ollama@v1
        with:
          model: glm-4.6:cloud

      - name: Get PR Diff
        id: diff
        run: |
          gh pr diff ${{ github.event.issue.number }} > /tmp/pr_diff.txt
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: AI Review
        uses: ai-action/ollama-action@v1
        id: review
        with:
          model: glm-4.6:cloud
          prompt: |
            A user requested a review with: "${{ github.event.comment.body }}"

            **PR Diff:**
            $(cat /tmp/pr_diff.txt | head -2000)

            Provide a focused code review addressing any specific requests.
            If no specific request, give a comprehensive review.

            Format your response with:
            - Summary
            - Key findings (if any issues)
            - Recommendations
        env:
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}

      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const review = `${{ steps.review.outputs.response }}`;
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: `## 🤖 AI Review Response

            ${review}

            ---
            <sub>Powered by Ollama GLM-4.6 Cloud</sub>`
            });
